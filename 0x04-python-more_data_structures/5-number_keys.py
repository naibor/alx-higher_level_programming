#!/usr/bin/python3
def number_keys(a_dictionary):
    """ counts the keys in a dict
    Args:
        a_dictionary: the dict
    Returns:
        returns a dict
    """
    list_key = list(a_dictionary)
    length = len(list_key)
    return length
